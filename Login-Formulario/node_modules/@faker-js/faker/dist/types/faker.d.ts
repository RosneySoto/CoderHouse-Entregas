import type { LocaleDefinition } from '@faker-js/faker/definitions';
import type { KnownLocale } from '@faker-js/faker/locales';
import { Address } from '@faker-js/faker/modules/address';
import { Animal } from '@faker-js/faker/modules/animal';
import { Color } from '@faker-js/faker/modules/color';
import { Commerce } from '@faker-js/faker/modules/commerce';
import { Company } from '@faker-js/faker/modules/company';
import { Database } from '@faker-js/faker/modules/database';
import { Datatype } from '@faker-js/faker/modules/datatype';
import { _Date } from '@faker-js/faker/modules/date';
import { Fake } from '@faker-js/faker/modules/fake';
import { Finance } from '@faker-js/faker/modules/finance';
import { Git } from '@faker-js/faker/modules/git';
import { Hacker } from '@faker-js/faker/modules/hacker';
import { Helpers } from '@faker-js/faker/modules/helpers';
import { Image } from '@faker-js/faker/modules/image';
import { Internet } from '@faker-js/faker/modules/internet';
import { Lorem } from '@faker-js/faker/modules/lorem';
import { Mersenne } from '@faker-js/faker/modules/mersenne';
import { Music } from '@faker-js/faker/modules/music';
import { Name } from '@faker-js/faker/modules/name';
import { Phone } from '@faker-js/faker/modules/phone';
import { Random } from '@faker-js/faker/modules/random';
import { Science } from '@faker-js/faker/modules/science';
import { System } from '@faker-js/faker/modules/system';
import { Unique } from '@faker-js/faker/modules/unique';
import { Vehicle } from '@faker-js/faker/modules/vehicle';
import { Word } from '@faker-js/faker/modules/word';
import type { LiteralUnion } from '@faker-js/faker/utils/types';
export declare type UsableLocale = LiteralUnion<KnownLocale>;
export declare type UsedLocales = Partial<Record<UsableLocale, LocaleDefinition>>;
export interface FakerOptions {
    locales: UsedLocales;
    locale?: UsableLocale;
    localeFallback?: UsableLocale;
}
export declare class Faker {
    locales: UsedLocales;
    private _locale;
    private _localeFallback;
    get locale(): UsableLocale;
    set locale(locale: UsableLocale);
    get localeFallback(): UsableLocale;
    set localeFallback(localeFallback: UsableLocale);
    readonly definitions: LocaleDefinition;
    readonly fake: Fake['fake'];
    readonly unique: Unique['unique'];
    readonly mersenne: Mersenne;
    readonly random: Random;
    readonly helpers: Helpers;
    readonly datatype: Datatype;
    readonly address: Address;
    readonly animal: Animal;
    readonly color: Color;
    readonly commerce: Commerce;
    readonly company: Company;
    readonly database: Database;
    readonly date: _Date;
    readonly finance: Finance;
    readonly git: Git;
    readonly hacker: Hacker;
    readonly image: Image;
    readonly internet: Internet;
    readonly lorem: Lorem;
    readonly music: Music;
    readonly name: Name;
    readonly phone: Phone;
    readonly science: Science;
    readonly system: System;
    readonly vehicle: Vehicle;
    readonly word: Word;
    constructor(opts: FakerOptions);
    /**
     * Creates a Proxy based LocaleDefinition that virtually merges the locales.
     */
    private initDefinitions;
    /**
     * Sets the seed or generates a new one.
     *
     * Please note that generated values are dependent on both the seed and the
     * number of calls that have been made since it was set.
     *
     * This method is intended to allow for consistent values in a tests, so you
     * might want to use hardcoded values as the seed.
     *
     * In addition to that it can be used for creating truly random tests
     * (by passing no arguments), that still can be reproduced if needed,
     * by logging the result and explicitly setting it if needed.
     *
     * @param seed The seed to use. Defaults to a random number.
     * @returns The seed that was set.
     *
     * @example
     * // Consistent values for tests:
     * faker.seed(42)
     * faker.datatype.number(10); // 4
     * faker.datatype.number(10); // 8
     *
     * faker.seed(42)
     * faker.datatype.number(10); // 4
     * faker.datatype.number(10); // 8
     *
     * @example
     * // Random but reproducible tests:
     * // Simply log the seed, and if you need to reproduce it, insert the seed here
     * console.log('Running test with seed:', faker.seed());
     */
    seed(seed?: number): number;
    /**
     * Sets the seed array.
     *
     * Please note that generated values are dependent on both the seed and the
     * number of calls that have been made since it was set.
     *
     * This method is intended to allow for consistent values in a tests, so you
     * might want to use hardcoded values as the seed.
     *
     * In addition to that it can be used for creating truly random tests
     * (by passing no arguments), that still can be reproduced if needed,
     * by logging the result and explicitly setting it if needed.
     *
     * @param seedArray The seed array to use.
     * @returns The seed array that was set.
     *
     * @example
     * // Consistent values for tests:
     * faker.seed([42, 13, 17])
     * faker.datatype.number(10); // 4
     * faker.datatype.number(10); // 8
     *
     * faker.seed([42, 13, 17])
     * faker.datatype.number(10); // 4
     * faker.datatype.number(10); // 8
     *
     * @example
     * // Random but reproducible tests:
     * // Simply log the seed, and if you need to reproduce it, insert the seed here
     * console.log('Running test with seed:', faker.seed());
     */
    seed(seedArray: number[]): number[];
    /**
     * Set Faker's locale
     *
     * @param locale The locale to set (e.g. `en` or `en_AU`, `en_AU_ocker`).
     */
    setLocale(locale: UsableLocale): void;
}
